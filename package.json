{
  "name": "games-of-life",
  "description": "is a Mathematical solution to any Game of Life variation",
  "version": "0.5.1",
  "main": "src/games-of-life.js",
  "scripts": {
    "build": "npm test && npm run browserify && npm run minify && npm run jekyll; git status",
    "push": "git push origin master && npm run gh-pages_push",
    "pull": "git pull origin master && npm run gh-pages_pull",
    "browserify": "for x in demo dist; do npm run browserify_$x; done",
    "browserify_dist": "browserify -r ./src/${npm_package_name}.js:${npm_package_name} -o dist/${npm_package_name}.js",
    "browserify_demo": "NODE_ENV=production NODE_PATH=src browserify demo/hexagonal/index.js -t babelify -o gh-pages/js/demo-hexagonal.js",
    "check-deps": "npm outdated",
    "generateDemoSVG": "NODE_PATH=src demo/hexagonal/generateSVG.js > gh-pages/svg/HexagonalGameOfLife.svg",
    "gh-pages_data": "npm run gh-pages_data_package.json; npm run gh-pages_data_tags.json",
    "gh-pages_data_package.json": "cp package.json gh-pages/_data/",
    "gh-pages_data_tags.json": "wget https://api.github.com/repos/fibo/${npm_package_name}/tags -O gh-pages/_data/tags.json",
    "gh-pages_push": "git subtree --prefix gh-pages push origin gh-pages",
    "gh-pages_pull": "git subtree --prefix gh-pages pull origin gh-pages",
    "homepage": "echo \"---\ntitle: $npm_package_name\n---\" > gh-pages/index.md; cat README.md >> gh-pages/index.md",
    "jekyll": "npm run homepage; npm run generateDemoSVG npm run gh-pages_data; cd gh-pages; jekyll build; cd ..",
    "lint": "npm run lint_demo; npm run lint_src; npm run lint_test",
    "lint_demo": "standard demo/*",
    "lint_src": "standard src/*",
    "lint_test": "cd test; standard --global describe --global it; cd -",
    "minify": "cd dist; uglifyjs ${npm_package_name}.js --source-map ${npm_package_name}.map --output ${npm_package_name}.min.js --compress --mangle --preamble \"// Software: ${npm_package_name}\n// Homepage: ${npm_package_homepage}\n// License: ${npm_package_license}\"; cd -",
    "postupdate-deps": "npm test && git commit -am 'updated deps' || git checkout -- package.json",
    "postversion": "git push origin v${npm_package_version}; npm publish; npm run push",
    "start": "NODE_PATH=src budo demo/hexagonal/index.js --live --open -- -t babelify",
    "test": "mocha --bail --require should --reporter min --recursive",
    "update-deps": "npm update --save --dev"
  },
  "pre-commit": [
    "check-deps",
    "lint",
    "test"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/fibo/games-of-life.git"
  },
  "keywords": [
    "math",
    "functional",
    "game-of-life"
  ],
  "author": {
    "name": "Gianluca Casati",
    "url": "http://g14n.info"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fibo/games-of-life/issues"
  },
  "homepage": "http://g14n.info/games-of-life",
  "devDependencies": {
    "browserify": "13.x",
    "mocha": "3.x",
    "pre-commit": "1.x",
    "should": "11.x",
    "standard": "8.x",
    "svgx": "^0.6.1",
    "uglify-js": "2.x",
    "zeroconf-redux": "^0.8.1"
  }
}
